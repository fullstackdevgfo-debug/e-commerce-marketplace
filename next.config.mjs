/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,

    images: {
    domains: ["gfofireequipments.com"],
  },
};

export default nextConfig;
